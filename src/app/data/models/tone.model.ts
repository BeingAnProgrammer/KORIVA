/**
 * The reference design's reused hue vocabulary — tag pills, category dots,
 * trend-card icons, and staleness badges all pick from this same set.
 * 'accent' is the primary brand indigo/violet, the rest are the fixed
 * green/amber/orange/rose/blue hues added to the token system.
 */
export type Tone = 'accent' | 'green' | 'amber' | 'orange' | 'rose' | 'blue';
